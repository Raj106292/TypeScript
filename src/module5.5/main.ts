import { addNumber as addNumber2, subtract } from './module';

const addNumber = (num1: number, num2: number, num3: number): number => {
    return num1 + num2 + num3;
}

const res = addNumber2(4, 6);